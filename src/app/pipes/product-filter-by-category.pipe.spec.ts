import { ProductFilterByCategoryPipe } from './product-filter-by-category.pipe';

describe('ProductFilterByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
